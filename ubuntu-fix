uname -a
lsb_release -a

sudo ufw status verbose

cat /proc/sys/vm/swappiness
sudo vim /etc/sysctl.conf
# Decrease swap usage to a more reasonable level
vm.swappiness=10

sudo apt-get purge unity-webapps-common xul-ext-unity xul-ext-websites-integration
sudo apt-get purge apport
sudo apt-get install unity-tweak-tool

gsettings set com.canonical.desktop.interface scrollbar-mode normal

#E: encountered a section with no package: header
#E: problem with Mergelist/var/lib/apt/lists
sudo rm -Rf /var/lib/apt/lists/* -vf

#Brightness is reset to Maximum on restart
sudo apt-get install xbacklight
Open the Startup Applications Preferences menu.
Click the Add button and add the following information:
Name: Brightness Command: xbacklight -set 60
Replace 60 with whatever brightness level you prefer.

sudo apt-get remove --purge libtotem-plparser18 totem totem-common totem-mozilla thunderbird firefox flashplugin-installer
sudo apt-get autoremove
sudo apt-get install binutils autoconf gcc g++ gdb dkms build-essential ufw gimp scrot htop wireshark curl wget zsh vim gnuchess xboard ccache valgrind anki p7zip-rar p7zip-full evince unity-tweak-tool vlc gawk ubuntu-restricted-extras git vimx meld

#*Please note that if using this ppa I would *not* try upgrading to 14.10/15.04, ect. Do a fresh install instead. The intent here is just for users wishing to stay on 14.04* If upgrading anyway use ppa-purge *First*
sudo ppa-purge ppa:mc3man/trusty-media
sudo ppa-purge ppa:videolan/stable-daily

sudo find /mnt/0/r -iname '*on7p8oqfv80*' -exec ls -alF {} +

#Ubuntu 14.04.3 freeze/hang on shutdown
sudo vi /etc/default/grub
#GRUB_CMDLINE_LINUX_DEFAULT="quiet splash" ->
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash acpi=force"
sudo update-grub

sudo addgroup --system admin
sudo adduser r admin
sudo chgrp admin 0
sudo chmod g+w,o-w 0

#Remove firefox and all associated language packs
sudo apt-get --purge autoremove firefox
dpkg --list | grep firefox
sudo apt-get --purge autoremove <leftover>

sudo apt-get remove --purge webaccounts-extension-common
sudo apt-get remove --purge unity-scope-musicstores unity-scope-musique unity-scope-tomboy 

sudo vim /etc/rc.local
echo 55 > /sys/class/backlight/intel_backlight/brightness

%admin ALL=(ALL) NOPASSWD: /sbin/shutdown

sudo apt-get purge linux-image-3.19.0-{39,43,47,49,51}-generic linux-image-extra-3.19.0-{39,43,47,49,51}-generic linux-headers-3.19.0-{39,43,47,49,51} linux-headers-3.19.0-{39,43,47,49,51}-generic 
